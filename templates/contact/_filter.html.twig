{{ form_start(form) }}
    {{ form_row(form.stringSearch) }}
    <h6>Entreprise</h6>
    {{ form_row(form.entreprise) }}
    <h6>Promotion</h6>
    {{ form_row(form.promotion) }}

    <button type="submit" class="btn btn-primary">Filtrer </button>
{{ form_end(form) }}